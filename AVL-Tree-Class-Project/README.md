# AVL-Tree-Class-Project
This project was created for CSC345 (Analysis of Discrete Structures) and it takes in an input file and creates an AVL tree based on the file. The tree is then inorderly traversed and each node is printed then height of the tree is printed as well.

The main file is 'avl_tree.java'.
